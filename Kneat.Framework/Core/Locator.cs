using System;
namespace Kneat.Framework.Core
{
    public enum Locator
    {
        Id,
        Name,
        ClassName,
        CssSelector,
        XPath,
        TagName,
        LinkText,
        PartialLinkText
    }
}
